#!/usr/bin/make -f
export DH_VERBOSE = 1

export PYBUILD_NAME=truenas-tui

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_build:
	python3 i18n/gen_locale.py
	dh_auto_build

override_dh_auto_test:
