[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "truenas-tui"
description = "TrueNAS Terminal User Interface"
requires-python = ">=3.11"
license = {text = "MIT"}
dynamic = ["version"]
dependencies = ["truenas-api-client"]

[project.scripts]
truenas-tui = "truenas_tui.main:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["truenas_tui*"]

[tool.setuptools.package-data]
truenas_tui = ["i18n/*.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "tests"]


[tool.mypy]
python_version = "3.11"
strict = true
files = ["truenas_tui", "i18n/update_translations.py"]
exclude = ["^tests/"]

[[tool.mypy.overrides]]
# deep-translator ships no type information and is only imported when the
# translation script actually needs to reach Google Translate.
module = "deep_translator"
ignore_missing_imports = true


[tool.ruff]
line-length = 88
# Directories that pytest/sys.path put on the import path, so ruff's isort
# classifies `mock_session` and `update_translations` as first-party.
src = [".", "tests", "i18n"]

[tool.ruff.lint]
select = [
    # pycodestyle
    "E",
    # Pyflakes
    "F",
    # isort
    "I",
]

[tool.ruff.lint.pycodestyle]
# The formatter wraps code at `line-length`; E501 only flags what the formatter
# cannot wrap (long strings, comments, docstrings) beyond this limit.
max-line-length = 120

[tool.ruff.lint.isort]
force-sort-within-sections = true
